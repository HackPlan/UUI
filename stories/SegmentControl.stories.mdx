import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { SegmentControl } from '../src';
import { useState } from 'react';

<Meta title="SegmentControl" component={SegmentControl} />

# 分段控制器 SegmentControl


## 简单使用

<Preview>
  <Story name="SegmentControl">
    {() => {
      const [value, setValue] = useState('hourly')
      return (
        <SegmentControl
          options={[
            { label: 'Hourly', value: 'hourly' },
            { label: 'Daily', value: 'daily' },
            { label: 'Monthly', value: 'monthly' },
          ]}
          label={'SegmentControl'}
          value={value}
          onChange={(value) => { console.log(value); setValue(value) }}
        />
      )
    }}
  </Story>
</Preview>

### Props

<Props components={{ SegmentControl }} />