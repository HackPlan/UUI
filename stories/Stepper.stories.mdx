import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Stepper } from '../src';
import { useState } from 'react';

<Meta title="Stepper" component={Stepper} />

# 计数器 Stepper


### 简单使用

<Preview>
  <Story name="Stepper">
    {() => {
      const [n, setN] = useState(null)
      return (
        <Stepper
          value={n}
          min={-10}
          max={10}
          onChange={(value) => {
            setN(value)
          }}
        >
        </Stepper>
      )
    }}
  </Story>
</Preview>

### 按钮位置

可以选择控制按钮的位置，设置 `controlPosition={'right'}` 使控制按钮显示在右侧

> 设置不同的按钮位置时，组件内部的 Nodes 结构会有所不同。
> 使用右侧控制按钮布局时，两个控制按钮会被 `RightControlsContainer` 的 Node 包一层，如果你需要自定义组件的样式，要注意一下这个多出来的 Node。

<Preview>
  <Story name="Stepper with controls right position">
    {() => {
      const [n, setN] = useState(null)
      return (
        <Stepper
          controlPosition={'right'}
          value={n}
          onChange={(value) => {
            setN(value)
          }}
        >
        </Stepper>
      )
    }}
  </Story>
</Preview>

### Props

<Props components={{ Stepper }} />