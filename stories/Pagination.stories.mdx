import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Pagination, PageInfo, PageList, PageJumper, PageSize } from '../src';
import { useState, useMemo } from 'react';
import { range } from 'lodash';

<Meta title="Pagination" component={Pagination} />

# 分页 Pagination

### Pagination

<Preview>
  <Story name="Pagination">
    {() => {
      const [pagination, setPagination] = useState({
        offset: 0,
        limit: 5,
        count: 163,
      })
      return (
        <div>
          <div className="my-4">
            <div>{JSON.stringify(pagination, null, 2)}</div>
          </div>
          <Pagination
            value={pagination}
            onChange={(value) => { setPagination(value) }}
          >
            <PageSize pageSizeOptions={[10, 30, 50, 100, 200]} />
            <PageList />
            <PageJumper labelText="跳转至" />
            <PageInfo />
          </Pagination>
          <PageInfo />
        </div>
      )
    }}
  </Story>
</Preview>

### Props

<Props components={{ Pagination, PageInfo, PageList, PageJumper, PageSize }} />