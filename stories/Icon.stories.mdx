import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Icon } from '../src';
import { IconSource, IconSvgProps, IconSize, IconIconGallery } from './Icon.stories.tsx';

<Meta title="Icon" component={Icon} />

# 图标 Icon

渲染图标工具，这个组件本身没有任何预置的图表库。

> 文档演示图片来自网络： https://www.iconfont.cn/collections/detail?cid=15329


### 多种数据源导入为图标

```typescript
const icon1 = require('./assets/icon_xxx.png');
import icon2, { ReactComponent as icon3 } from './assets/icon_xxx.svg';
/**
 * icon1: string
 * icon2: string
 * icon3: SvgrComponent
 */
```

可以打开控制台工具看看这三种方式渲染出来的 DOM 区别。

<Canvas>
  <Story name="Icon">{IconSource()}</Story>
</Canvas>

### 内联 SVG 支持传入更多参数

<Canvas>
  <Story name="Icon with svg props">{IconSvgProps()}</Story>
</Canvas>


### 自定义图标大小

<Canvas>
  <Story name="Icon with custom size">{IconSize()}</Story>
</Canvas>

### IconGallery

使用 `IconGallery` 工具组织管理图标库

<Canvas withSource="open">
  <Story name="Icon with IconGallery">{IconIconGallery()}</Story>
</Canvas>


### Props

<ArgsTable components={{ Icon }} />