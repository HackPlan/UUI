import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { HTMLSelect } from '../src';
import { useState } from 'react';

<Meta title="Select" components={{ HTMLSelect }} />

# 选择器 Select


## HTML 原生选择器 HTMLSelect

<Preview>
  <Story name="HTMLSelect">
    {() => {
      const options = [
        { label: 'Cat', value: 'cat' },
        { label: 'Dog', value: 'dog' },
        { label: 'Parrot', value: 'parrot' },
      ]
      const [value, setValue] = useState('dog')
      return (
        <HTMLSelect options={options} value={value} onChange={(value) => { setValue(value) }}/>
      )
    }}
  </Story>
</Preview>

### Props

<Props components={{ HTMLSelect }} />