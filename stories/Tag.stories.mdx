import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Tag, TagNodes } from '../src';
import { useState } from 'react';

<Meta title="标签 Tag" component={Tag} />

# 标签 Tag

### Tag

<Canvas>
  <Story name="Tag">
    <Tag>Tag</Tag>
  </Story>
</Canvas>

### 自定义样式

<Canvas>
  <Story name="Custom Style 1">
    <Tag
      customize={{
        Root: {
          extendClassName: 'flex flex-col',
          extendStyle: {
            background: '#CCC'
          },
          extendChildrenAfter: <div>append element</div>
        },
        Content: {
          extendClassName: 'text-green-800'
        },
      }}
    >ABC</Tag>
  </Story>
  <Story name="Custom Style 2">
    <Tag
      onClick={() => {}}
      customize={{
        Root: {
          overrideClassName: 'uui-tag inline-block p-6 rounded-full bg-gray-600',
        }
      }}
    >ABC</Tag>
  </Story>
</Canvas>

### Props

<ArgsTable components={{ Tag }} />