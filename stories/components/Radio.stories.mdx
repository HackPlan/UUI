import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Radio, RadioGroup } from '../../src';
import { useState } from 'react';

<Meta title="组件/单选框 Radio" component={Radio} />

# 单选框 Radio


### 单个单选框

<Canvas>
  <Story name="Radio">
    <Radio label={'Radio'} value={"radio-value-1"} />
  </Story>
</Canvas>

### 一组单选框

<Canvas>
  <Story name="Group of Radio">
    {() => {
      const [n, setN] = useState("a")
      return (
        <RadioGroup name="radio1" value={n} onChange={(value) => { setN(value) }}>
          <Radio label={'Apple'} value={'a'} customize={{ Root: { extendClassName: "mr-4" }}}></Radio>
          <Radio label={'Banana'} value={'b'} customize={{ Root: { extendClassName: "mr-4" }}}></Radio>
          <Radio label={'Cherry'} value={'c'} customize={{ Root: { extendClassName: "mr-4" }}}></Radio>
        </RadioGroup>
      )
    }}
  </Story>
</Canvas>

### Props

<ArgsTable components={{ Radio, RadioGroup }} />